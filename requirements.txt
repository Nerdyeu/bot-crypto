# Runtime dependencies
ccxt>=4.2,<5            # unified exchange access (used from Milestone 2)
pandas>=2.1,<3          # data & indicators (used from Milestone 3/4)
pydantic>=2.6,<3        # config validation
pydantic-settings>=2.2  # load/validate config from env / .env
python-dotenv>=1.0      # .env support
requests>=2.31          # optional Telegram alerts (Milestone 6+)

# Development / testing
pytest>=8.0
